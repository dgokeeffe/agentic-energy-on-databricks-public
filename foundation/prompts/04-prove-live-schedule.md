# Prompt 04 — prove the live schedule

Use this prompt in a **fresh conversation**. This stage is facilitator-only and
ends with the schedule paused and snapshot-safe defaults restored.

```text
This is a fresh conversation. You are assisting the workshop facilitator with the temporary live-schedule stage only. Read AGENTS.md, foundation/AGENTS.md, foundation/Instructions.md, PRE-REQUISITES.md, foundation/skills/02-live-landing/SKILL.md, foundation/skills/03-evidence-gates/SKILL.md, foundation/deployment-gates.md gate 6 through Rollback, and the accepted gate record from stage three. Treat every nemweb_foundation link as facilitator-only; do not route participants there. Before any action, require dated approval for AEMO use, network access, live deployment, the temporary schedule change, and the selected target. Every workspace-aware command must include --profile daveok. Require both deployment-controlled values nemweb_mode=live and allow_live_nemweb=true; participants cannot set them. If and only if the facilitator explicitly authorises each external action, validate and deploy live mode, unpause the five-minute job, observe and capture three consecutive scheduled cycles using exact orchestration run IDs and exact pipeline update IDs, pause the job again, restore snapshot/false defaults, and redeploy. Use the authoritative capture and validation scripts in nemweb_foundation/scripts; do not copy them. Fail closed on missing expectations, duplicate keys, source-ahead-of-Bronze, ambiguous update identity, or invalid cycle spacing. Do not invent data when AEMO publishes nothing new, use a full refresh, include private workspace details in evidence, or leave the schedule running. Return exact commands, exit codes, the reviewed non-sensitive evidence path, rollback actions, final paused state, restored defaults, and the human decision, then stop.
```
