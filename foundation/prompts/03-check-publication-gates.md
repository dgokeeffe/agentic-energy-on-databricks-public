# Prompt 03 — check publication gates

Use this prompt in a **fresh conversation**. This stage ends with a recorded
decision on whether the foundation is ready for a separately approved live
trial. It does not enable live mode or change a schedule.

```text
This is a fresh conversation. You are assisting the workshop facilitator with the publication-gate stage only. Read AGENTS.md, foundation/AGENTS.md, foundation/Instructions.md, PRE-REQUISITES.md, foundation/skills/03-evidence-gates/SKILL.md, foundation/deployment-gates.md gates 3–5, and the snapshot evidence from stage two. Treat every nemweb_foundation link as facilitator-only; do not route participants there. Confirm the exact completed pipeline update, landed-to-Bronze reconciliation, correction ordering, Gold natural-key and effective-run uniqueness, watermarks, expectation metrics, T+1 labelling, and metric-to-Gold reconciliation. With explicit current authorisation for each operation, run the semantic and analyst SQL checks from the authoritative solution project. Every workspace-aware command must include --profile daveok. Do not publish or update Genie or dashboard assets until every statement is SUCCEEDED; then inspect the deployed resources only if separately authorised. Do not deploy, access live NEMWEB, run another refresh, unpause a schedule, expose a secret, treat snapshot rows as live, or weaken a check. Return exact commands, exit codes, gate results, prepared substitutes required, and the human accept, send-back, reject, or stop decision, then stop so the next stage can start in a fresh conversation.
```
