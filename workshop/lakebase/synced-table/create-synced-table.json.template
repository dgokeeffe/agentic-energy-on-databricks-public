{
  "spec": {
    "source_table_full_name": "${UC_CATALOG}.${UC_SCHEMA}.gold_nem_app_region_status",
    "primary_key_columns": ["serving_key"],
    "scheduling_policy": "TRIGGERED",
    "branch": "projects/${LAKEBASE_PROJECT}/branches/${LAKEBASE_BRANCH}",
    "postgres_database": "${LAKEBASE_DATABASE_NAME}",
    "create_database_objects_if_missing": true,
    "new_pipeline_spec": {
      "storage_catalog": "${UC_STORAGE_CATALOG}",
      "storage_schema": "${UC_STORAGE_SCHEMA}"
    }
  }
}
