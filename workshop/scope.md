# Workshop scope and historical evidence

Reviewed against baseline `0345aea12a1638c14126a1003bff692fa1b6018a`.
The history below is evidence of intent, not code to restore.

- `7a94164^:miniwiki/decisions/track-structure.md`: self-contained, self-paced
  tracks, ordered human review, fixed AEST and UTC semantics.
- `7a94164^:miniwiki/decisions/workshop-flow.md`: capture intent, approve a bounded
  brief, implement, test, review evidence, and leave a handoff.
- `7a94164^:lane_b_engineering/assets/task-plan.md`: real red-to-green evidence
  and a separate assessment of an agent's tool choice, scope and trajectory.
- `7a94164^:miniwiki/features/genie-miniwiki-integration.md`: questions, semantic
  context, benchmarks, freshness and governance; do not guess Genie capability.
- `7a94164^:nemweb_foundation/genie/benchmark_questions.json`: effective
  intervention, price/demand, signed fuel generation, constraints and source-sign
  interconnectors. The T+1 availability benchmark depends on a removed product
  and is excluded here.
- `7a94164^:miniwiki/features/track-c-exploratory-investigation.md`: observe,
  investigate, review uncertainty, save a reviewed note, follow up. Old prepared
  Genie UI and CDC architecture are not required by the current baseline.

`git log --all -S 'initial_supply'` found no implementation; the spaced phrase
only appears in baseline scope exclusions. Historical Markdown has no initial
supply definition. The provisional interpretation is documented prominently in
the exercise: regional signed actual SCADA aggregation. No additional engineering
labs, ML, T+1 availability, app integration or investigation CDC are inferred.

Acceptance: one runnable learner checkpoint with an absent solution; the same
behavioral examples pass on a separate solution checkpoint; one isolated remote
pipeline executes the new MV and its results reconcile to corrected unit output;
retained baseline checks pass; preparation supplies bounded Genie questions and
reference SQL; a fresh-checkout facilitator runbook covers configuration, reset,
verification, teardown and exact evidence. Publication/sync/app actions remain
explicit. PRs prepare the work for human review, without merging or moving tags.
