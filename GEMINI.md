# GEMINI.md

Agent guidance for this repo lives in one canonical file:

**→ [`AGENTS.md`](AGENTS.md)**

Read it before attempting a deployment: inside a CoDA container there is no
`databricks auth login` step and no PAT to find, and the deploy-time detail is in
[`.claude/skills/deploying-from-coda/SKILL.md`](.claude/skills/deploying-from-coda/SKILL.md).

Edit guidance in `AGENTS.md`, not here — this stub only points to it so the
per-harness files never drift.
